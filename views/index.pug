extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  else
    p You are currently logged in as #{user.username}
    a(href="/aquariums") Take me to aquariums
    br
    a(href="/logout") Logout
